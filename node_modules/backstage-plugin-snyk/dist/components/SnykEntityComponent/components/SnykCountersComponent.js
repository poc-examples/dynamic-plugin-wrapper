import React from "react";
import { useSnykCounterStyle } from './SnykCounterStyle';
export const SnykCounter = ({ issuesCount, }) => {
    const classes = useSnykCounterStyle();
    const defaultIssueCount = { critical: 0, high: 0, medium: 0, low: 0 };
    const children = Object.entries({ ...defaultIssueCount, ...issuesCount }).map(([key, value]) => {
        const cssSuffix = value > 0 ? key : 'disabled';
        return React.createElement("li", { key: key, className: classes.item + ' ' + classes[`item_${cssSuffix}`] },
            React.createElement("div", { className: classes.count },
                React.createElement("span", null, value)),
            React.createElement("abbr", { className: classes.text + ' ' + classes[`item_${cssSuffix}_text`], title: `${key} issues` }, key.slice(0, 1).toUpperCase()));
    });
    return React.createElement("ul", { className: classes.root }, children);
};
//# sourceMappingURL=SnykCountersComponent.js.map