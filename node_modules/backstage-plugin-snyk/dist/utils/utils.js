export const extractTargetShortname = (rawName) => {
    let name = "";
    const tokenizedName = rawName.split(":");
    if (tokenizedName.length === 2) {
        name = tokenizedName[1];
    }
    else {
        name = rawName;
    }
    return name;
};
export const extractProjectIdFromAnnotations = (annotationsObject) => {
    let projectIds = Object.keys(annotationsObject).filter((key) => key.includes("snyk.io/project-ids"));
    if (projectIds && projectIds.length === 1) {
        projectIds = annotationsObject["snyk.io/project-ids"]
            .split(",")
            .map((x) => x.replace(" ", ""));
    }
    else {
        throw new Error("unexpected project Ids syntax");
    }
    return projectIds;
};
//# sourceMappingURL=utils.js.map